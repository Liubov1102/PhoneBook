import Notiflix from 'notiflix';

Notiflix.Notify.init({
    width: '280px',
    position: 'center-top',
    fontSize: `16px`,
    distance: '10px',
    opacity: 1,
    timeout: 3000,
    clickToClose: `true`,
});